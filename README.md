# KLSGIT18-Devops
This is my simple practice Dev Repoditory
MCA Department
This is how We connect the git and clone repo in vs
Hey this is scrum changes
Hey made some changes
new changes
Added New Github branch and cart story
Dockerfile:
FROM nginx:alpine
COPY index.html /usr/share/nginx/html/index.html

commands:

docker ps to check container are active
docker build -t image_name to create image
docker run -d -p 8080:80 -- name docker-demo docker_demo_image to create container and run docker image
docker images to see all images
